meta:
  name: digest
  toolchain: python  # ← обязательно нужно добавить
  version: '3.11'

build:
  requirementsPath: requirements.txt
  useCache: false

run:
  scriptName: app.py
  command: python app.py
  workdir: my-news-dashboard
  persistenceMount: /data
  containerPort: 80
  env: []
